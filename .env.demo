# Modalità DEMO: `npm run demo` (vite --mode demo).
# Svuotando le variabili Supabase l'app parte in locale (dati nel browser),
# senza login e senza toccare il cloud. Il `.env` normale resta intatto.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GOOGLE_CLIENT_ID=
